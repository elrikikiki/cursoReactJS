import React from 'react'

const Prueba = () => {
    /* const [count, setCount] = useState(0)
    const [texto, setTexto] = useState(0)
    const plus = () => {
        if (count >= 10) { 

        }else {
            setCount(count + 1)
        }
        
    }
    const subtract = () => {
        if (count === 0) {
            //no quiere q reste mas, por eso no pongo nada
        } else {
            setCount(count -1)
        }
        
       
    }
    const agregar = () => {
        setTexto(count)
       }  */
  return (
    <div>
    
    {/* <button onClick={plus}>+</button>
    <div>{count}</div>
    <button onClick={subtract}>-</button>
    <button onClick={agregar}>agregar</button>
    <div>{texto}</div> */}

    </div>
  )
}

export default Prueba